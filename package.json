{
  "name": "draft-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @printdraft/cli dev",
    "demo:dev": "cd demo && node ../packages/cli/bin/draft.js dev",
    "build": "pnpm -r --filter=\"./packages/**\" run build",
    "test": "pnpm -F @printdraft/parser test",
    "release": "bumpp package.json packages/*/package.json --all"
  },
  "devDependencies": {
    "bumpp": "catalog:dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "puppeteer",
      "@puppeteer/browsers"
    ]
  },
  "packageManager": "pnpm@10.33.2"
}
